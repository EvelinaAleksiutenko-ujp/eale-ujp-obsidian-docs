1. I found out that our segmentation model can be used for classification and the output of it will probably look like A:\DATA\Segmentation\CONTOUR_CORRECTION\HaN_model_classifications.csv. It can be used for oulier detection step.
	UPD: I took a look on A:\DATA\Segmentation\CONTOUR_CORRECTION\CONTOUR_CORRECTION_structure_map and I am not sure that the label 'BODY' should be included in every class. And probably it is not connected to the script that we have in auto-segmentace. Probably it is just mapping based on the RTSTRUCT info.
~~TODO~~: say this to Jana and Abhiram as they are now asking guys from Decin for outlier detection.